{
  "name": "websocket-reconnect",
  "version": "1.0.7",
  "description": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rimraf dist && tsc",
    "tsc": "tsc",
    "tsc:watch": "tsc -w",
    "prettier": "node ./node_modules/prettier/bin-prettier.js --parser=typescript --single-quote --trailing-comma=all --write --print-width=120 'src/**/*.ts'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/z-hao-wang/websocket-reconnect.git"
  },
  "keywords": [
    "nodejs",
    "websocket",
    "reconnect",
    "websocket reconnect",
    "typescript",
    "typing"
  ],
  "author": "Hao Wang",
  "license": "MIT",
  "dependencies": {
    "events": "^3.3.0",
    "ws": "^8.11.0"
  },
  "devDependencies": {
    "@types/events": "^3.0.0",
    "@types/lodash": "^4.14.123",
    "@types/ws": "^6.0.1",
    "prettier": "^1.16.4",
    "rimraf": "^3.0.2"
  }
}
